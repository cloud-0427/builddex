// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    alias(libs.plugins.android.application) apply false
    id 'io.github.gradle-nexus.publish-plugin' version '2.0.0'
}

def centralUsername = providers.gradleProperty('centralUsername')
        .orElse(providers.environmentVariable('CENTRAL_USERNAME')).orNull
def centralPassword = providers.gradleProperty('centralPassword')
        .orElse(providers.environmentVariable('CENTRAL_PASSWORD')).orNull
group = providers.gradleProperty('central.group').getOrElse('io.github.cloud-0427')
version = providers.gradleProperty('jiagu.version').getOrElse('0.1.21')

// Sonatype's Central Portal OSSRH compatibility endpoint. Credentials are kept
// outside version control and are only needed by the remote publishing tasks.
nexusPublishing {
    repositories {
        sonatype {
            nexusUrl.set(uri('https://ossrh-staging-api.central.sonatype.com/service/local/'))
            snapshotRepositoryUrl.set(uri('https://central.sonatype.com/repository/maven-snapshots/'))
            username = centralUsername
            password = centralPassword
        }
    }
}
